esta carpeta es donde flask buscara de manera automatica las plantillas
 es por eso que se debe llamar de un nombre en concreto

Jinja es un motor de plantillas de python que ya viene junto con flask
from flask import Flask, render_template


